// def len(s: String): Int = s.length()

// def numberOfS(s: String): Int = {
//   s.length() - s.replaceAll("s", "").length()
// }

// def is_odd(i: Int): Boolean = {
//   i % 2 == 1
// }

/*
List("scala", "rust", "ada").filter(w => len(w) < 5)

List("rust", "ada").filter(w => numberOfS(w) >= 3)

List(5, 1, 2, 4, 0).filter(is_odd)

List(5, 1, 2, 4, 0).filter(i => i > 4)
 */
